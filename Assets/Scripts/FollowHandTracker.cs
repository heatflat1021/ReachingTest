using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using UnityEngine.XR;
using Valve.VR;
using UnityEngine.UI;

public class FollowHandTracker : MonoBehaviour
{

    void Start(){ }

    // Update is called once per frame
    void Update()
    {
        // this.transform.position = TrackerManager.Instance.HandTrackerPosition();
    }
}
